---
export interface Props {
  title: string;
  description: string;
  href: string;
  gradientClass?: string; // Tailwind gradient background class e.g. 'from-orange-400 to-red-500'
}
const { title, description, href, gradientClass = "from-orange-500 to-pink-500" } = Astro.props as Props;
---
<a href={href} class={"group block rounded-2xl p-6 md:p-8 bg-gradient-to-br text-white shadow-lg hover:shadow-2xl transition-shadow duration-300 " + gradientClass}>
  <h2 class="text-2xl md:text-3xl font-bold mb-3 group-hover:translate-x-1 transition-transform duration-200">{title} →</h2>
  <p class="opacity-90 leading-relaxed">{description}</p>
</a> 